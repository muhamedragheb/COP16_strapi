'use strict';

/**
 * entity-classification controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::entity-classification.entity-classification');
